
# 🧭 บทนำ: การวิเคราะห์ข้อความเชิงบริบท (Contextual Text Analysis)

**แนวคิดหลัก:**
คือการทำความเข้าใจ “ความหมายของข้อความ” โดยพิจารณาบริบท (Context) ที่คำหรือประโยคปรากฏร่วมกัน ไม่ใช่เพียงการนับคำหรือความถี่ของคำ

ตัวอย่างเช่น

> คำว่า “bank” อาจหมายถึง “ธนาคาร” หรือ “ริมฝั่งน้ำ”
> การเข้าใจบริบทจึงเป็นสิ่งจำเป็นต่อการตีความที่ถูกต้อง

ในยุคก่อนคอมพิวเตอร์ไม่สามารถเข้าใจบริบทได้อย่างแท้จริง เพราะพิจารณาข้อความแบบแยกคำ (Bag-of-Words)
แต่แนวทางใหม่คือการ **แทนความหมายของคำในรูปแบบตัวเลข (Vector Representation)**
ทำให้เครื่องสามารถคำนวณความใกล้เคียงเชิงความหมายได้


# ⚙️ Text Embedding คืออะไร

**Text Embedding** คือกระบวนการแปลงข้อความ (เช่น คำ ประโยค เอกสาร) ให้อยู่ในรูปของเวกเตอร์ในปริภูมิมิติสูง (High-Dimensional Vector Space)

**หลักการสำคัญ:**

* ข้อความที่ “มีความหมายใกล้กัน” จะถูกแทนด้วยเวกเตอร์ที่ “อยู่ใกล้กัน”
* ทำให้สามารถวัดความสัมพันธ์ทางความหมายด้วยวิธีทางคณิตศาสตร์ เช่น

  * **Cosine Similarity** → ใช้วัดมุมระหว่างเวกเตอร์
  * **Euclidean Distance** → ใช้วัดระยะทางระหว่างเวกเตอร์

ตัวอย่างแนวคิด:

> “ครู” → ใกล้กับ “อาจารย์” และ “ผู้สอน” มากกว่า “นักเรียน”


# 🧩 ทฤษฎีที่เป็นพื้นฐานทางวิชาการ

## 1. **Distributional Semantics Theory** (Firth, 1957)

> “You shall know a word by the company it keeps.”

* ทฤษฎีนี้ตั้งอยู่บนสมมติฐานว่า “คำที่ปรากฏในบริบทเดียวกันมักมีความหมายคล้ายกัน”
* เป็นรากฐานสำคัญของแนวคิดการเรียนรู้เชิงการกระจาย (Distributional Representation)
* ตัวอย่างเช่น คำว่า “แมว” มักปรากฏร่วมกับคำว่า “เลีย”, “ขน”, “สัตว์เลี้ยง” ซึ่งช่วยให้ระบบเข้าใจความหมายของ “แมว” ได้จากบริบท


## 2. **Vector Space Model (VSM)**

* เป็นการแทนข้อความในรูปของเวกเตอร์
* ทำให้ข้อความสามารถถูกประมวลผลทางคณิตศาสตร์ เช่น การคำนวณความคล้ายคลึง
* แนวคิดนี้เป็นจุดเริ่มต้นของการใช้ embedding ในการวิเคราะห์ภาษา


## 3. **Word Embedding Models**

| โมเดล                                  | แนวคิด                                                  | จุดเด่น                                                          |
| -------------------------------------- | ------------------------------------------------------- | ---------------------------------------------------------------- |
| **Word2Vec** (Mikolov et al., 2013)    | ใช้ Neural Network เรียนรู้คำจากบริบท (CBOW/Skip-gram)  | เข้าใจความสัมพันธ์เชิงความหมาย เช่น “king - man + woman = queen” |
| **GloVe** (Pennington et al., 2014)    | ใช้สถิติการปรากฏร่วมของคำทั่วคลังข้อมูล (Co-occurrence) | สร้าง embedding จาก global context                               |
| **FastText** (Bojanowski et al., 2016) | พิจารณา subword (คำย่อย)                                | รองรับคำใหม่ (OOV words) ได้ดี เช่น คำไทยที่ไม่อยู่ในพจนานุกรม   |


## 4. **Contextual Embeddings**

เป็นการแทนคำที่คำนึงถึง “บริบทของประโยค” โดยใช้ **Deep Neural Networks**

| โมเดล                             | ลักษณะสำคัญ                                    | จุดเด่น                                   |
| --------------------------------- | ---------------------------------------------- | ----------------------------------------- |
| **ELMo**                          | ใช้ Bi-directional LSTM                        | คำเดียวกันมี embedding ต่างกันในบริบทต่าง |
| **BERT**                          | ใช้ Transformer แบบสองทิศทาง (Bidirectional)   | เรียนรู้จากทั้งด้านซ้ายและขวา             |
| **GPT**                           | ใช้ Transformer แบบหนึ่งทิศทาง (Left-to-right) | เน้นการสร้างข้อความต่อเนื่อง              |
| **Sentence Transformers (SBERT)** | ปรับจาก BERT สำหรับการเปรียบเทียบประโยค        | ใช้ใน Semantic Search, Clustering ได้ดี   |

ตัวอย่าง:

> “bank” ใน “river bank” ≠ “bank” ใน “bank loan”
> BERT สามารถสร้าง embedding ที่แตกต่างกันได้ตามบริบท


# 🔬 แนวคิดทฤษฎีขั้นสูง

## 1. **Manifold Hypothesis**

* สมมติฐานว่าข้อความหรือคำในภาษาธรรมชาติมีโครงสร้างซับซ้อนอยู่ในพื้นที่มิติสูง
* การเรียนรู้ embedding คือการ “สกัดโครงสร้างเชิงความหมาย” ที่ซ่อนอยู่ใน manifold นั้นออกมา

## 2. **Transfer Learning**

* การนำ embedding ที่ผ่านการฝึกในคลังข้อมูลขนาดใหญ่ (เช่น Wikipedia หรือ Common Crawl) มาใช้ในงานใหม่ (Downstream Tasks) เช่น

  * การจำแนกอารมณ์ (Sentiment Classification)
  * การสรุปข้อความ (Summarization)

## 3. **Explainability in Embeddings**

* การพยายามตีความว่ามิติของเวกเตอร์แต่ละแกน “หมายถึงอะไร” ทางภาษาศาสตร์ เช่น
  แกนหนึ่งอาจแทน “ความเป็นเพศชาย–หญิง” หรือ “ระดับบวก–ลบทางอารมณ์”


# 💼 การประยุกต์ใช้ Text Embedding

| การประยุกต์                         | คำอธิบาย                                                                               |
| ----------------------------------- | -------------------------------------------------------------------------------------- |
| **Semantic Search**                 | ค้นหาข้อความที่มี “ความหมายใกล้เคียง” เช่น “รถยนต์ไฟฟ้า” ใกล้กับ “ยานยนต์พลังงานสะอาด” |
| **Clustering / Topic Discovery**    | จัดกลุ่มข้อความที่มีบริบทคล้ายกัน เช่น การจัดกลุ่มข่าว                                 |
| **Recommendation Systems**          | แนะนำบทความหรือข้อมูลตามความสนใจ                                                       |
| **Semantic Similarity Measurement** | ตรวจจับความคล้ายคลึงของเนื้อหาหรือการลอกงาน                                            |
| **LLM Context Understanding**       | ใช้ embedding เป็นส่วนหนึ่งของโครงสร้างโมเดลภาษาขนาดใหญ่ เช่น GPT, Claude, LLaMA       |


# 🧠 สรุปเชิงทฤษฎี

1. **Text Embedding คือสะพานเชื่อมระหว่างภาษาและคณิตศาสตร์**
   ทำให้คอมพิวเตอร์เข้าใจ “ความหมาย” แทนที่จะมองแค่ “คำ”

2. **Contextual Embedding คือวิวัฒนาการขั้นสูง**
   ที่ทำให้คำเดียวกันสามารถมีความหมายต่างกันได้ตามบริบท

3. **Embedding เป็นรากฐานของระบบ NLP ยุคใหม่ทั้งหมด**
   ตั้งแต่ Search Engine, Chatbot, ไปจนถึงโมเดลภาษาอย่าง GPT


# 🔎 สรุปท้ายบท (Key Takeaways)

* **Distributional Semantics** คือหัวใจของการเรียนรู้ความหมาย
* **Embedding = การแทนความหมายในรูปตัวเลข**
* **Context Matters:** คำมีความหมายขึ้นอยู่กับบริบท
* **Modern NLP = Contextual Embedding + Deep Learning**
* **ความเข้าใจ embedding คือก้าวแรกสู่การใช้ LLM อย่างมีประสิทธิภาพ**


ได้เลยครับอาจารย์ CJ — ด้านล่างนี้คือ **ส่วนที่ 2: ตัวอย่างและการประยุกต์ใช้ (Applications)**
ของหัวข้อ “การวิเคราะห์ข้อความเชิงบริบทโดยใช้ Text Embedding”
ซึ่งจัดทำในเชิงทฤษฎีเพื่อประกอบการฟังบรรยายให้เข้าใจเชิงแนวคิดและหลักวิชาการ
เหมาะสำหรับการอธิบายหลังจากผู้เรียนเข้าใจพื้นฐานของ **Text Embedding** แล้ว (ต่อจากส่วนที่ 1)

---

# 🧩  การประยุกต์ใช้ (Applications of Text Embedding)

Text Embedding ไม่ได้เป็นเพียงเทคนิคทางคณิตศาสตร์หรือภาษาศาสตร์คำนวณเท่านั้น แต่เป็น **แกนกลางของเทคโนโลยีประมวลผลภาษาธรรมชาติ (Natural Language Processing – NLP)** เกือบทั้งหมดในยุคปัจจุบัน
โดยเฉพาะระบบที่ต้อง “เข้าใจความหมาย” ของข้อความแทนที่จะมองแค่รูปแบบของคำ

 

## 1. 🔍 **Semantic Search (การค้นหาตามความหมาย)**

### แนวคิด:

ต่างจากการค้นหาแบบดั้งเดิม (Keyword-based Search) ที่มองหาคำตรงตัว
**Semantic Search** ใช้ embedding เพื่อค้นหาข้อความที่ *มีความหมายใกล้เคียง* กัน

### ตัวอย่าง:

* ค้นคำว่า “รถยนต์ไฟฟ้า”
  → ระบบสามารถคืนผลลัพธ์ที่มีข้อความว่า “ยานยนต์พลังงานสะอาด” หรือ “EV” ได้
  แม้ไม่มีคำว่า “รถยนต์ไฟฟ้า” ปรากฏอยู่ตรง ๆ

### ทฤษฎีที่เกี่ยวข้อง:

* ใช้ **Cosine Similarity** เพื่อวัดความใกล้ชิดของเวกเตอร์ข้อความ
* พื้นฐานจากแนวคิด **Semantic Vector Space Model (Turney & Pantel, 2010)**

### ประโยชน์:

* ใช้ในระบบฐานความรู้ (Knowledge Base)
* ระบบ Chatbot หรือ Question Answering
* ค้นหาเอกสารวิชาการหรือข่าวสารที่มีความหมายใกล้เคียงกัน

 

## 2. 🧠 **Clustering and Topic Discovery (การจัดกลุ่มข้อความ / ค้นหาหัวข้อ)**

### แนวคิด:

เมื่อแต่ละข้อความถูกแปลงเป็นเวกเตอร์แล้ว
สามารถใช้เทคนิคทางคณิตศาสตร์ เช่น

* **K-means clustering**
* **Hierarchical clustering**
  เพื่อจัดกลุ่มข้อความที่มีความหมายคล้ายคลึงกันให้อยู่ในกลุ่มเดียวกัน

### ตัวอย่าง:

* การจัดกลุ่มข่าว:

  * ข่าวกลุ่ม “เศรษฐกิจ”, “เทคโนโลยี”, “สิ่งแวดล้อม” จะมี embedding ใกล้กัน
* การจัดกลุ่มบทวิจารณ์สินค้าโดยอัตโนมัติ

### ทฤษฎีที่เกี่ยวข้อง:

* การแทนข้อความด้วย embedding ทำให้การวัดระยะทาง (semantic distance) มีความหมายทางเนื้อหา
* เป็นการนำ **Manifold Hypothesis** มาใช้ในทางปฏิบัติ (ข้อมูลภาษามีโครงสร้างภายในที่ไม่เชิงเส้น)

### ประโยชน์:

* ใช้ค้นหา “หัวข้อซ่อนอยู่” ในข้อมูลจำนวนมาก (Topic Modeling)
* ช่วยสรุปหรือทำ visualization ของข้อมูลข้อความ (เช่น UMAP หรือ t-SNE plot)

 

## 3. 🎯 **Recommendation Systems (ระบบแนะนำเนื้อหา)**

### แนวคิด:

Embedding สามารถใช้จับ “ความคล้ายคลึงของเนื้อหา”
เพื่อแนะนำสิ่งที่ใกล้เคียงกับความสนใจของผู้ใช้

### ตัวอย่าง:

* ผู้ใช้ชอบอ่านบทความเรื่อง “ความมั่นคงทางไซเบอร์”
  → ระบบสามารถแนะนำบทความที่มี embedding ใกล้กัน เช่น “ภัยคุกคามทางดิจิทัล” หรือ “การโจมตีแบบฟิชชิง”
* แพลตฟอร์มข่าว / e-learning / วารสารวิชาการ

### ทฤษฎีที่เกี่ยวข้อง:

* **Content-based Filtering:** ใช้ embedding ของเนื้อหาเป็นฐาน
* **Collaborative Filtering:** ใช้ embedding ของผู้ใช้และเนื้อหาร่วมกัน

### ประโยชน์:

* เพิ่มประสิทธิภาพของระบบแนะนำโดยไม่ต้องใช้เพียง metadata
* สามารถเรียนรู้ความสนใจเชิงบริบทของผู้ใช้ได้ลึกกว่าเดิม

 

## 4. 🧮 **Semantic Similarity Measurement (การวัดความคล้ายคลึงเชิงความหมาย)**

### แนวคิด:

เป็นการคำนวณระดับ “ความใกล้เคียงของความหมาย” ระหว่างสองข้อความ
โดยใช้เวกเตอร์แทนข้อความทั้งสองแล้ววัดความสัมพันธ์เชิงมุมหรือระยะทาง

### ตัวอย่าง:

* การตรวจจับการลอกงานเชิงความหมาย (Semantic Plagiarism Detection)
* การเปรียบเทียบประโยคในระบบสรุปข้อความ (Text Summarization)
* การตรวจจับการซ้ำซ้อนของคำถามในฐานข้อมูล Q&A

### ทฤษฎีที่เกี่ยวข้อง:

* ใช้ **Cosine Similarity** หรือ **Dot Product** เป็นหลักในการวัด
* อาศัย embedding ที่ได้จาก **Sentence-BERT (Reimers & Gurevych, 2019)** ซึ่งออกแบบมาเพื่อความสัมพันธ์เชิงความหมายระหว่างประโยค

### ประโยชน์:

* ใช้ในงานวิจัยด้าน **Paraphrase Identification**, **Textual Entailment**, และ **Information Retrieval**

 

## 5. 🤖 **LLM Context Understanding (การใช้ในโมเดลภาษาขนาดใหญ่)**

### แนวคิด:

ในโมเดลภาษาเชิงลึก เช่น **BERT, GPT, LLaMA**, หรือ **Gemini**,
embedding เป็นองค์ประกอบหลักของ “ชั้นแรก” (Input Representation Layer)
ที่แปลงคำหรือประโยคให้เป็นเวกเตอร์ก่อนเข้าสู่ Transformer

### ตัวอย่าง:

* เมื่อโมเดลอ่านประโยค “แมวไล่จับหนู”
  → แต่ละคำจะถูกแทนด้วย embedding
  → โมเดลสามารถเข้าใจความสัมพันธ์ “subject–verb–object” ได้จากบริบท

### ทฤษฎีที่เกี่ยวข้อง:

* **Transformer Architecture (Vaswani et al., 2017)**
  ใช้ Attention Mechanism เพื่อเชื่อมโยงบริบทของคำทั้งหมดในประโยค
* **Contextual Embedding:** คำเดียวกันอาจมี embedding ต่างกันในบริบทต่าง ๆ

### ประโยชน์:

* ทำให้โมเดลสามารถ “เข้าใจบริบท” ได้ในระดับลึก
* เป็นพื้นฐานของระบบ ChatGPT, Claude, Gemini, และโมเดลภาษาอื่น ๆ

 

## 6. 🔬 **งานประยุกต์ในเชิงวิจัย (Research Applications)**

| สาขา                      | การประยุกต์                                                                                                                        |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| **Cybersecurity**         | วิเคราะห์เนื้อหาการโจมตีจากข้อความ (เช่น phishing email หรือ malware report) โดยใช้ embedding เพื่อจับความสัมพันธ์ของคำศัพท์เทคนิค |
| **Education Analytics**   | วิเคราะห์การตอบแบบสอบถามของผู้เรียนด้วย contextual embedding เพื่อระบุอารมณ์หรือเจตคติ                                             |
| **Healthcare NLP**        | วิเคราะห์รายงานทางการแพทย์ เช่น อาการหรือผลตรวจ โดยใช้ embedding เชื่อมโยงคำทางคลินิก                                              |
| **Social Media Analysis** | ตรวจจับแนวโน้มความคิดเห็น (sentiment trend) หรือการเผยแพร่ข้อมูลผิดพลาด (misinformation propagation)                               |

 

# 🧠 สรุปท้ายส่วน (Key Insights)

1. **Text Embedding คือรากฐานของการ “เข้าใจเนื้อหา” ไม่ใช่แค่ “อ่านข้อความ”**
   → จาก keyword-based → meaning-based analysis

2. **การประยุกต์ใช้ครอบคลุมทั้งระดับระบบ (Search, Recommendation)**
   และระดับวิจัย (Semantic Analysis, Context Understanding)

3. **Embedding ทำให้เกิดการเรียนรู้เชิงความหมายแบบคณิตศาสตร์**
   ซึ่งเปิดประตูสู่ระบบอัจฉริยะที่เข้าใจภาษาใกล้เคียงมนุษย์มากขึ้น

 
 

# 🧪  Workshop – การสร้างและวิเคราะห์ Text Embedding

## 🎯 วัตถุประสงค์ของ Workshop

เมื่อจบกิจกรรม ผู้เรียนจะสามารถ:

1. เข้าใจวิธีการแปลงข้อความเป็นเวกเตอร์เชิงความหมาย
2. วัดและตีความความคล้ายคลึงของข้อความในเชิงคณิตศาสตร์ได้
3. เห็นความแตกต่างระหว่าง **การวิเคราะห์แบบเดิม (Keyword)** กับ **การวิเคราะห์แบบบริบท (Embedding)**
4. ตระหนักถึงความสำคัญของ Contextual Embedding ในระบบสมัยใหม่ เช่น Search, Clustering และ Chatbot

 

## ⚙️ ส่วนที่ 3.1: เตรียมเครื่องมือและข้อมูล

### เครื่องมือที่ใช้

* **Python / Google Colab**
* ไลบรารีหลัก:

  * `sentence-transformers`
  * `numpy`
  * `scikit-learn` (optional สำหรับ visualization)

### ตัวอย่างชุดข้อมูล

ให้ผู้เรียนทดลองกับข้อความภาษาไทยที่มีบริบทแตกต่างกัน:

```python
texts = [
    "รถยนต์ไฟฟ้าเป็นมิตรต่อสิ่งแวดล้อม",
    "ยานยนต์พลังงานสะอาดกำลังได้รับความนิยม",
    "ธนาคารให้บริการสินเชื่อบ้าน",
    "แม่น้ำมีน้ำมากในฤดูฝน",
    "รถยนต์ใช้น้ำมันเชื้อเพลิง"
]
```

 
## 🧠 ส่วนที่ 3.2: การสร้าง Sentence Embeddings

ใช้โมเดลที่รองรับหลายภาษา เช่น
`paraphrase-multilingual-MiniLM-L12-v2` จาก **Sentence Transformers**
ซึ่งสามารถจัดการข้อความภาษาไทยได้ดี

```python
from sentence_transformers import SentenceTransformer

# โหลดโมเดล
model = SentenceTransformer('paraphrase-multilingual-MiniLM-L12-v2')

# แปลงข้อความเป็นเวกเตอร์
embeddings = model.encode(texts)
print(embeddings.shape)   # ตัวอย่าง: (5, 384)
```

**คำอธิบาย:**

* ข้อความแต่ละประโยคจะถูกแทนด้วยเวกเตอร์ขนาด 384 มิติ
* เวกเตอร์เหล่านี้อยู่ในพื้นที่เชิงความหมาย (Semantic Space)
* ข้อความที่มีความหมายใกล้กันจะมีเวกเตอร์อยู่ใกล้กันในพื้นที่นั้น

 

## 📏 ส่วนที่ 3.3: การวัดความคล้ายคลึงของข้อความ (Semantic Similarity)

ใช้ **Cosine Similarity** เพื่อคำนวณ “มุม” ระหว่างเวกเตอร์
มุมยิ่งเล็ก → ความหมายยิ่งใกล้กัน

```python
from sentence_transformers import util

similarity = util.cos_sim(embeddings, embeddings)
print(similarity)
```

### การตีความค่า Similarity

| ค่าความคล้าย (cosine) | ความสัมพันธ์เชิงความหมาย |
| --------------------- | ------------------------ |
| 0.8 – 1.0             | เหมือนหรือใกล้เคียงมาก   |
| 0.5 – 0.8             | ค่อนข้างคล้าย            |
| 0.2 – 0.5             | คล้ายเพียงเล็กน้อย       |
| < 0.2                 | ไม่คล้ายกันเลย           |

 

## 🔍 ส่วนที่ 3.4: วิเคราะห์ผลลัพธ์

ตัวอย่างผลที่คาดหวัง:

| ข้อความคู่                                                                     | ค่า Cosine | การตีความ                                       |
| ------------------------------------------------------------------------------ | ---------- | ----------------------------------------------- |
| “รถยนต์ไฟฟ้าเป็นมิตรต่อสิ่งแวดล้อม” ↔ “ยานยนต์พลังงานสะอาดกำลังได้รับความนิยม” | 0.85       | มีความหมายใกล้กันสูง                            |
| “รถยนต์ไฟฟ้า…” ↔ “ธนาคารให้บริการสินเชื่อบ้าน”                                 | 0.12       | ความหมายต่างกันมาก                              |
| “รถยนต์ไฟฟ้า…” ↔ “รถยนต์ใช้น้ำมันเชื้อเพลิง”                                   | 0.65       | อยู่ในหัวข้อเดียวกันแต่มีความหมายตรงข้ามบางส่วน |

**อภิปราย:**

* ค่า similarity บ่งชี้ถึง “ระยะทางทางความหมาย”
* ระบบค้นหาความรู้ (Semantic Search Engine) สามารถใช้หลักนี้เพื่อค้นหาข้อความใกล้เคียงในเชิงบริบท

 

## 📊 ส่วนที่ 3.5 (เสริม): การมองภาพเชิงเวกเตอร์

สามารถใช้การลดมิติ (Dimensionality Reduction) เช่น **t-SNE** หรือ **UMAP** เพื่อแสดงตำแหน่งของเวกเตอร์แต่ละข้อความในพื้นที่ 2 มิติ

```python
from sklearn.manifold import TSNE
import matplotlib.pyplot as plt

tsne = TSNE(n_components=2, random_state=42)
reduced = tsne.fit_transform(embeddings)

plt.scatter(reduced[:,0], reduced[:,1])
for i, text in enumerate(texts):
    plt.annotate(text, (reduced[i,0], reduced[i,1]))
plt.title("Visualization of Sentence Embeddings")
plt.show()
```

**ผลลัพธ์:**

* ข้อความที่มีความหมายใกล้กันจะอยู่ใกล้กันในกราฟ
* แสดงให้เห็นโครงสร้าง “semantic clusters” อย่างชัดเจน

 

## 🧩 ส่วนที่ 3.6: สรุปการเรียนรู้จาก Workshop

| แนวคิดหลัก                                | คำอธิบาย                                                                     |
| ----------------------------------------- | ---------------------------------------------------------------------------- |
| **Embedding = การแทนข้อความเป็นเวกเตอร์** | เพื่อให้คอมพิวเตอร์สามารถ “เข้าใจ” ความหมายเชิงคณิตศาสตร์                    |
| **Context Matters**                       | ข้อความที่มีคำเหมือนกันแต่บริบทต่างกันจะได้ embedding ต่างกัน                |
| **Similarity = ความใกล้กันทางความหมาย**   | วัดด้วย cosine similarity                                                    |
| **Visualization**                         | ทำให้เห็นว่า embedding ของข้อความที่มีความหมายคล้ายกันจะอยู่ในพื้นที่ใกล้กัน |
| **ประยุกต์ใช้จริง**                       | ใช้ในระบบค้นหาความหมาย, การจัดกลุ่มเนื้อหา, การสรุปข้อมูล, Recommendation    |

 

## 📘 บทสรุปเชิงแนวคิด

* Text Embedding ทำให้ “ความหมาย” ของข้อความถูกแปลงเป็น “ข้อมูลเชิงคณิตศาสตร์”
* การวัดความคล้ายคลึงของเวกเตอร์เป็นวิธีที่สะท้อน “ความใกล้ชิดทางความหมาย” ได้อย่างแม่นยำ
* Contextual Embedding เช่น BERT ช่วยให้ระบบเข้าใจคำในบริบทได้ลึกยิ่งขึ้น
* การมอง embedding ในเชิงกราฟช่วยให้เข้าใจโครงสร้างของข้อมูลภาษาธรรมชาติ (semantic manifold)

 ## 🧪 **โค้ดสาธิต: Spam Detection ด้วย Sentence Embedding**

 
### 🧭 แนวคิดเชิงทฤษฎี

#### 🔹 เป้าหมาย

เราต้องการแยกข้อความออกเป็นสองกลุ่ม:

* **Spam:** มีเจตนาหลอกลวง, โปรโมชัน, การเสนอรางวัล, ลิงก์ให้คลิก
* **Ham:** ข้อความทั่วไปจากการสื่อสารหรือธุรกิจ

#### 🔹 หลักการของ Contextual Embedding

* แปลงข้อความแต่ละรายการเป็นเวกเตอร์ใน *semantic space*
* ข้อความที่มี “ความหมาย” คล้ายกันจะอยู่ใกล้กันในพื้นที่เวกเตอร์
* จากนั้นสามารถใช้เทคนิคทางคณิตศาสตร์ เช่น **cosine similarity**, **clustering**, หรือ **classification** เพื่อจำแนกกลุ่ม

 


ด้านล่างนี้เป็นโค้ด Python (รันได้ใน Jupyter / Colab)
ที่ใช้ **SentenceTransformer** ในการสร้าง embedding และวิเคราะห์เชิงบริบท

```python
# ============================================================
# Showcase: Contextual Spam Detection using Text Embedding
# ============================================================

!pip install -q sentence-transformers scikit-learn matplotlib

from sentence_transformers import SentenceTransformer
from sklearn.decomposition import PCA
from sklearn.preprocessing import LabelEncoder
from sklearn.neighbors import KNeighborsClassifier
import matplotlib.pyplot as plt
import numpy as np

# ------------------------------------------------------------
# 1. เตรียมข้อมูล
# ------------------------------------------------------------
data = {
    'label': ['spam', 'ham', 'ham', 'spam', 'ham', 'spam', 'ham', 'spam'],
    'text': [
        "Congratulations! You won a free iPhone. Click here to claim now!",
        "Hi John, can we reschedule our meeting tomorrow?",
        "Your Amazon order has been shipped successfully.",
        "Limited offer! Get a $1000 cash prize. Visit our website today!",
        "Lunch at 12:30? Let me know your preference.",
        "Earn money from home!!! Exclusive deal inside!!!",
        "Reminder: Your report is due by Monday.",
        "You’ve been selected for a free vacation trip to Hawaii!"
    ]
}

texts = data['text']
labels = data['label']

# ------------------------------------------------------------
# 2. สร้าง Sentence Embeddings
# ------------------------------------------------------------
model = SentenceTransformer('all-MiniLM-L6-v2')  # อังกฤษ
embeddings = model.encode(texts)

print("Embedding Shape:", embeddings.shape)

# ------------------------------------------------------------
# 3. การลดมิติ (Visualization)
# ------------------------------------------------------------
pca = PCA(n_components=2)
reduced = pca.fit_transform(embeddings)

# ------------------------------------------------------------
# 4. การแสดงผลการกระจายตัวของข้อความ
# ------------------------------------------------------------
plt.figure(figsize=(8,6))
for i, label in enumerate(labels):
    color = 'red' if label == 'spam' else 'blue'
    plt.scatter(reduced[i,0], reduced[i,1], color=color)
    plt.annotate(f"{label}", (reduced[i,0]+0.1, reduced[i,1]), fontsize=10)
plt.title("Semantic Space of Messages (Spam vs Ham)")
plt.xlabel("PCA Dimension 1")
plt.ylabel("PCA Dimension 2")
plt.grid(True)
plt.show()

# ------------------------------------------------------------
# 5. การฝึกโมเดลจำแนกเบื้องต้น (KNN)
# ------------------------------------------------------------
encoder = LabelEncoder()
y = encoder.fit_transform(labels)   # แปลง label เป็นตัวเลข

clf = KNeighborsClassifier(n_neighbors=3)
clf.fit(embeddings, y)

# ทดสอบข้อความใหม่
new_messages = [
    "Get rich quick! Limited time offer, claim your reward.",
    "Please review the attached project report for tomorrow’s meeting.",
    "Win a free laptop just by signing up!"
]

new_embeds = model.encode(new_messages)
preds = clf.predict(new_embeds)

for msg, pred in zip(new_messages, preds):
    print(f"[{msg}] → {encoder.inverse_transform([pred])[0]}")
```

 

### 📊 **ผลที่คาดหวัง**

เมื่อรันโค้ดนี้:

* กราฟ PCA จะแสดงให้เห็นว่า **ข้อความสแปม (สีแดง)** และ **ข้อความปกติ (สีน้ำเงิน)**
  กระจายอยู่คนละบริเวณในพื้นที่เวกเตอร์เชิงความหมาย
* โมเดล KNN จะสามารถจำแนกข้อความใหม่ได้อย่างถูกต้องตามบริบท เช่น

| ข้อความ                                                  | ผลลัพธ์ที่คาดหวัง |
| -------------------------------------------------------- | ----------------- |
| “Get rich quick! Limited time offer, claim your reward.” | spam              |
| “Please review the attached project report…”             | ham               |
| “Win a free laptop just by signing up!”                  | spam              |

 
### 🧠 **การตีความเชิงแนวคิด**

1. **ข้อความที่มีเจตนาโปรโมชันหรือรางวัล** จะมี embedding ที่คล้ายกัน
   → อยู่รวมกันในคลัสเตอร์เดียว (เช่น “win”, “offer”, “free”, “claim”)
2. **ข้อความเชิงธุรกิจ / การสื่อสารทั่วไป** จะอยู่ในอีกกลุ่มหนึ่ง
   → บริบทเช่น “meeting”, “report”, “reminder”
3. Embedding จึงทำให้โมเดลเข้าใจ *“เจตนา (intent)”* ของข้อความได้
   แม้ไม่มีคำว่า “spam” หรือ “promotion” ปรากฏอยู่ตรง ๆ

 

### 🔍 **ประโยชน์ในเชิงวิชาการและงานวิจัย**

| ด้าน                      | การประยุกต์                                              |
| ------------------------- | -------------------------------------------------------- |
| **Cybersecurity**         | ตรวจจับข้อความฟิชชิง (phishing) ที่ใช้ภาษาหลอกลวงแบบใหม่ |
| **Email Filtering**       | ระบบกรองอีเมลอัตโนมัติที่เข้าใจบริบทมากกว่า keyword      |
| **Social Media Analysis** | ตรวจจับ scam หรือ spam content ในโซเชียล                 |
| **NLP Research**          | ศึกษา embedding space สำหรับ text classification         |

